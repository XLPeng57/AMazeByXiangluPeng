<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.wm.cs.cs301.xianglupeng">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".gui.AMazeActivity"></activity>
        <activity android:name=".gui.GeneratingActivity"></activity>
        <activity android:name=".gui.PlayManuallyActivity"></activity>
        <activity android:name=".gui.WinningActivity"></activity>
        <activity android:name=".gui.PlayAnimationActivity"></activity>
        <activity android:name=".gui.LosingActivity"></activity>


    </application>

</manifest>